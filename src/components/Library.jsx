/**
 * @deprecated  This Library modal has been replaced by the inline list
 * inside Sidebar.jsx. Kept as a stub so any old import doesn't crash;
 * safe to delete entirely.
 */
export default function Library() { return null; }
